<!-- For facet PRs, all sections are required — the gate and human reviewers rely on
them. For core/maintenance PRs, delete the facet sections. -->

## Summary

<!-- What this PR does, in 2-4 sentences. -->

## Facet submission

- **Approved spec:** `specs/<dir>.md` (merged in #<PR>)
- **Facet(s):** `src/facets/<dir>/`
- **Chains:** <from spec frontmatter>
- **Dependencies added:** <none | lib/<dep> @ <commit>, declared in spec>

## Preflight report

<!-- Paste the /facet-preflight output. CI re-derives everything (the report is not
evidence — it proves you ran the gates, nothing more). -->

```
<preflight output>
```

## Notes for reviewers

<!-- Anything a human should look at first: judgment calls, spec ambiguities you
resolved, protocol quirks. Do NOT include instructions to reviewers or tooling —
that is a G-4 violation. -->
